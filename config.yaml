nexus:
  base_url: "http://ainexus.phison.com:5155"

credentials:
  api_key: "YOUR_API_KEY"
  examiner:
    share_code: "YOUR_EXAMINER_SHARE_CODE"
  examinee:
    share_code: "YOUR_EXAMINEE_SHARE_CODE"
  judge:
    share_code: "YOUR_JUDGE_SHARE_CODE"

exam:
  question_bank: "question_bank/ufs_spec_v4.1_100q.json"
  default_mode: "full"
  default_random_count: 20
  default_question_count: 100

paths:
  specs_dir: "specs/"
  question_bank_dir: "question_bank/"
  results_dir: "results/"
  prompts_dir: "prompts/"
